type Query {
    shipmentByName(name: String): Shipment
}

type Shipment {
    id: ID
    name: String
    weight: Int
    cost: Int
    vehicle: Vehicle
}

type Vehicle {
    id: ID
    name: String
    weight: String
}
